import { symbol } from "./symbol";
import type { Input as IconInput } from "../../index.marko"
export type Input = Omit<IconInput, `_${string}`>;
<evo-icon ...input _name="refresh-24" _size="24" _type="icon" _symbol=symbol/>
